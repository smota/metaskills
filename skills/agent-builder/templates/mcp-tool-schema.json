{
  "name": "example_lookup",
  "description": "Read-only lookup tool. Permission tier: read. Auth: none. Failure fallback: explain unavailable lookup and ask user for source data.",
  "inputSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "query": {
        "type": "string",
        "description": "Lookup query"
      }
    },
    "required": [
      "query"
    ]
  },
  "_meta": {
    "agentBuilder": {
      "permissionTier": "read",
      "confirmationRequired": false,
      "toolAbsentFallback": "Use manual source instructions.",
      "toolFailureFallback": "Report failure and ask for retry or source data.",
      "validation": "Call with sample query and verify structured result or clear error."
    }
  }
}
