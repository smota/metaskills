# Level 5 learning agent example

Current maturity: Level 5. Target maturity: Level 5.

Uses eval suite, local redacted feedback, benchmark report, and changelog-driven improvement loop.

Fallback: if telemetry is unavailable, use manual eval notes and changelog entries.
## Validation

- Portable without `metaskills` runtime.
- `AGENT.md` remains canonical.
- Missing capability fallback is explicit.
