# skill-builder notes

Source component: [`../../../components/skill-builder`](../../../components/skill-builder)

Component metadata: [`../../../components/skill-builder.metaskills.md`](../../../components/skill-builder.metaskills.md)

Mode: `reference-only`

Concepts to borrow:

- production-ready `SKILL.md` files
- strong activation descriptions
- progressive disclosure through supporting files
- intention-revealing file names
- conversion of sub-agents into skills
- keeping core instructions concise and moving details into references

Adaptation rules:

1. Keep `skill-creator` native to this repo under `skills/skill-creator`.
2. Keep all required install content inside `skills/skill-creator`.
3. Do not require the submodule during installation or runtime.
4. If upstream improvements are needed, branch inside `components/skill-builder` and send PRs there.
