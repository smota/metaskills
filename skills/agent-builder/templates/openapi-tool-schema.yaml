openapi: 3.1.0
info:
  title: Tool API
  version: 1.0.0
paths:
  /tool_action:
    post:
      operationId: tool_action
      summary: Clear description of the tool action
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                param1:
                  type: string
                  description: ...
              required:
                - param1
      responses:
        '200':
          description: Success response
